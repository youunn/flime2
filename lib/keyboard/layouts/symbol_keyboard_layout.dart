import 'package:flutter/material.dart';

class SymbolKeyboardLayout extends StatelessWidget {
  const SymbolKeyboardLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('symbol');
  }
}
